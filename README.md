The restaurant finder project is a React application that allows users to search for top restaurants in a specified city using the Google Maps API.

In the project directory, you can run:

### `npm start`

